def moving_avg(y, n):
    # calculates the moving average of the values in y with a window size of n. 
    # Returns a numeric array that has the same length as y with some None values at the
    # start and end of the array (see test cases)
    pass
    


def ols(y):
    # calculates the ols linear trend estimators of the values in y. 
    # Returns a numeric tuple of length two. The first value euqals the constant (a)
    # the second value equals the beta coefficient (b). 
    pass


    
    
    
